<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Imagem de fundo com arte do PetLar -->
    <ImageView
        android:id="@+id/imgFundoInicio"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/fundo_pets"
        android:scaleType="centerCrop"
        android:alpha="0.60"
        android:adjustViewBounds="true"
        android:contentDescription="Fundo com arte de pets" />

    <!-- Conteúdo principal por cima da imagem -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@android:color/transparent">

        <!-- Título da tela -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Pets disponíveis para adoção"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:padding="16dp"
            android:textColor="@android:color/black" />

        <!-- Lista de pets -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewPets"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:padding="8dp" />

        <!-- Texto quando não houver pets -->
        <TextView
            android:id="@+id/txtNenhumPet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Nenhum pet disponível no momento."
            android:gravity="center"
            android:padding="16dp"
            android:textSize="16sp"
            android:textColor="@android:color/black"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
